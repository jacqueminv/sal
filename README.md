A tiny utility to fetch Strava activities of the past year.

You'll need an [API Application](https://www.strava.com/settings/api) to use `sal`.

Once you got that, fetching your own Strava activities is done via: `SAL_CLIENT_ID=<your_client_id> SAL_CLIENT_SECRET_ID=<your_client_secret_id> sal`
